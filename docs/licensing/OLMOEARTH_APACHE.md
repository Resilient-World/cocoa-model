# OlmoEarth license boundary

| Component | License | Use |
|-----------|---------|-----|
| OlmoEarth pretrain weights (Allen AI) | Apache-2.0 | Optional backbone via `[olmoearth]` extra |

Install: `pip install -e ".[olmoearth]"`. Exposure backend `ensemble_v4` is opt-in and gated by benchmark promotion.

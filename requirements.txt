# Core geospatial stack
geopandas>=0.14
rasterio>=1.3
xarray>=2024.1
xee>=0.0.15
zarr>=2.17
dask[array]>=2024.1
numpy>=1.26
netcdf4>=1.6
earthengine-api>=0.1.390

# Deep learning
torch>=2.1
pytorch-lightning>=2.1
huggingface_hub>=0.24
einops>=0.7
torchgeo>=0.5
terratorch>=0.1
segmentation-models-pytorch>=0.3
# Galileo upstream package requires Python <3.12; on 3.12+ use vendored models.vendor.*
# galileo @ git+https://github.com/nasaharvest/galileo@main

# MLOps
mlflow>=2.10
dvc>=3.0

# Modeling & API
scikit-learn>=1.3  # HistGradientBoosting + early stopping (PSM/DML)
pandas>=2.1
fastapi>=0.110
uvicorn[standard]>=0.27
httpx>=0.27
pydantic-settings>=2.0
statsmodels>=0.14  # event-study OLS with cluster-robust SEs
pooch>=1.8
requests>=2.31

# ATTRICI v2.0.1 (GPLv3) — install only in isolated venv / optional extra; never import in src/
# attrici @ git+https://github.com/ISI-MIP/attrici.git@v2.0.1

# Optional (Callaway-Sant'Anna upgrade — not pinned yet):
# differences>=0.2.0
linearmodels>=6.0

# Optional: local ERA5 GeoTIFF export (pip install geemap or pip install -e ".[gee]")
# geemap>=0.30
